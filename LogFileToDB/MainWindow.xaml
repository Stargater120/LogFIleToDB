<Window x:Class="LogFileToDB.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogFileToDB"
        xmlns:src="clr-namespace:Core.Models;assembly=Core"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <src:TimeRange x:Key="timeRange"/>
    </Window.Resources>
    <Grid>
        <Button Click="AddDataThroughFile" Content="Button" HorizontalAlignment="Left" Margin="195,85,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="startDate" HorizontalAlignment="Left" Margin="327,251,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        <TextBox x:Name="endDate" HorizontalAlignment="Left" Margin="492,251,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="153">
            <TextBox.Text>
            <Binding Source="{StaticResource timeRange}" Path="Begin">
                    <Binding.ValidationRules>
                        <ExceptionValidationRule/>
                        <local:DatetimepickerValidation></local:DatetimepickerValidation>
                    </Binding.ValidationRules>
            </Binding>
            </TextBox.Text>
        </TextBox>
    </Grid>
</Window>
